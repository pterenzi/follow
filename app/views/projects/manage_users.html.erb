<%= render :partial=>"layouts/moldura" %>
<% content_for :main do %>
<% #TODO fazer o jquery com on opme %>
<h1>Projeto : <%= @project.name  %></h1>
<p/><br/>

 Selecionados <br/>
<div id="group">
	<span id="selected_users"></span>
<% for u in @included do%>
  <span id="selected_<%=u.id %>">
  <a href="#" onClick="remove_user(<%=  @project.id %>,<%= u.id %>, '<%= u.name %>','projects' )"><%= u.name %></a><br/>
    </span>    
<% end %>
</div>
<br/>

 Fora do projeto <br/>
(clique no name do usuario para incluí-lo no projeto)<br/>
<div id="group">
	<span id="not_selected_users"></span>
    <% for u in @not_selected do %>
      <span id="not_selected_<%= u.id %>">
      <a href="#" onClick="include_user(<%=  @project.id %>,<%= u.id %>, '<%= u.name %>','projects' )"><%= u.name %></a><br/>
      </span>
    <% end %>
</div>


<%= link_to t(:back), project_path(@project), :class=> 'awesome small blue button' %>

<% end %>